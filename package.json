{
  "name": "login",
  "version": "1.0.0",
  "description": "login form with bootstrap & sass",
  "main": "index.js",
  "scripts": {
    "sass-dev": "sass --watch --update --style=expanded assets/scss:assets/css",
    "sass-prod": "sass --no-source-map --style=compressed assets/scss:assets/css"
  },
  "repository": {
    "type": "git",
    "url": "login"
  },
  "author": "rex vero",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.67.0"
  }
}
